import './bootstrap';
import { createApp } from 'vue'; // Si usas Vue

const app = createApp({});

// Montar componentes Vue si es necesario
// app.component('example-component', ExampleComponent);

app.mount('#app');
